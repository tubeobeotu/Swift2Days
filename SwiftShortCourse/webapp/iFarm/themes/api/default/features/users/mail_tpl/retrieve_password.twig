<!DOCTYPE html>
<html>
<head>
    <title>Xác nhận reset mật khẩu tài khoản</title>
</head>
<body>
<h3>Hệ thống vừa nhận được yêu cầu reset mật khẩu đối với tài khoản <b>{{ user_email }}</b></h3>
<p>Để hoàn thiện xác nhận reset mật khẩu, vui lòng truy cập vào đường link:</p>
<p>
    <a href="{{ getConfig('domain') }}/user/reset-password/{{ user_id }}/{{ token }}">
        {{ getConfig('domain') }}/user/reset-password/{{ user_id }}/{{ token }}
    </a>
</p>

</body>
</html>